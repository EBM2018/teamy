version: "3"
services:
  web:
    image: ebm2018/teamy:latest
    # depends_on:
    #   - mongodb
    environment:
      - MONGODB_URI=mongodb://mongodb/teamy
    networks:
      local:
      ebm-filrouge:
        aliases:
          - teamy
  # mongodb:
  #   image: mongo
  #   networks:
  #     - local
networks:
  local:
  ebm-filrouge:
    external: true
