// const UserData = require('../../../services/user/model');

const controller = {
  index(req, res) {
    return res.status(200).send('INDEX TO BE DEVELOPED');
  },

  eleve_list(req, res) {
    return res.send('LIST NOT IMPLEMENTED YET');
  },
};

module.exports = controller;
